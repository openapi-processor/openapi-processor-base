openapi: 3.1.0
info:
  title: response success
  version: 1.0.0

paths:

  /informational-is-success:
    get:
      responses:
        '100':
          headers:
            Location:
              schema:
                type: string

  /redirection-is-success:
    get:
      responses:
        '302':
          headers:
            Location:
              schema:
                type: string
