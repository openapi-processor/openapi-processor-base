openapi: 3.2.0
info:
  title: unreferenced schema
  version: 1.0.0
components:
  schemas:
    Foo:
      description: unreferenced
      type: object
      properties:
        bar:
          type: string
