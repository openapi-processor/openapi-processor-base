openapi: 3.0.3
info:
  title: unreferenced schema
  version: 1.0.0

paths: {}

components:
  schemas:

    Foo:
      description: unreferenced
      type: object
      properties:
        bar:
          type: string
